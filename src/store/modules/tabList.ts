import setting from '@/store/modules/type'
const state = {
  tabList: [{ title: '首页', key: '1', closable: false, path: '/home' }]
}
const getters = {}
const mutations = {}
const actions = {}
export default {
  namespaced: true,
  state,
  getters,
  mutations
}
